export { NotificationServiceProvider } from './NotificationServiceProvider';
export { useNotificationService } from './NotificationServiceContext';

// модуль уведомлений для всего приложения
/**
 * ТЗ
 *
 * нужно сделать сервис уведомлений, который можно вызывать из любого места, в любом сервисе
 * Обертка из контекста само собой
 * Две функции: create и clear и сделать хук для компонентов
 *
 *
 *
 *
 */
